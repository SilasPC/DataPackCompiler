
import file from './test'

/*fn getAllPlayers() {
	return @a[]
}*/

fn x(): int {
	a += b
	/say hello
	/say hello
	// x()
	file::b + 1;
	/say helloo
	return file::a *
		3
}

let a = b;
const b = 2;
let yr = y()

fn y(): int {return 1}
