
use super::std
// use subMod

event abc
on abc {
	/say abc fired
}

struct A {}
struct B implements A {}

//let lol = false

fn x(): void {
	let t = 0
	t += (2+t) * t
	/execute as @a positioned 1 2 3 align x facing entity @s feet run say hello!
	y()
}

fn y(): void {}
