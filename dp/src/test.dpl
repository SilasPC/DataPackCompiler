
use super::std
// use subMod

event abc
on abc {
	/say abc fired
}

struct A {}
struct B implements A {}

fn x(): bool {
	let t: bool = 0
	t += (2+t) * t
	/execute as @a positioned 1 2 3 align x facing entity @s feet run say hello!
	y()
	let lol = 1 > 2
	let lol2 = y()
	if (lol) {
		/say hello
	}
	return lol2
}

#[todo]
fn y(): bool {
	return 1 != 2
}
